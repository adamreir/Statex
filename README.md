# Statex
Statex is a Stata package that streamlines the process of generating clean, publication-ready LaTeX tables from your Stata output.

Installation: 
```
net install statex, from("https://raw.githubusercontent.com/adamreir/statex/main")
```

Tested on Stata MP 16.1
